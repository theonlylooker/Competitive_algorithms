var reverseList = function (head) {
  const list = new ListNode();
  let result = list;
  const reverseRecursive = (node) => {
    if (!node) {
      return;
    }
    if (node.next) {
      list;
    }
  };
};
